---
phase: 5
plan: 04
type: feature
wave: 4
depends_on:
  - 05-03
files_modified:
  - CMakeLists.txt
  - include/astrabot/metamod/nav_loader.hpp
  - src/adapter/metamod/nav_loader.cpp
  - src/adapter/metamod/plugin_runtime.cpp
  - tests/nav_loader_tests.cpp
  - tests/nav_diagnostics_tests.cpp
  - docs/source-manifest.json
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - TEST-02
---

<objective>
Add the adapter read-only file boundary, map-aware diagnostics, and Phase 5
verification fixtures without changing the six-export ABI or adding Nav
authoring behavior.
</objective>

<context>
Read `05-CONTEXT.md`, all prior Phase 5 summaries/plans, Metamod-P public file
and map boundaries, and `C_CPP_REFACTOR_RULES.md`. Use bounded adapter file
reads and the immutable publisher from 05-03. Do not call private GameDLL
symbols or modify source `.nav` files.
</context>

<tasks>

### Task 1: Write failing adapter/diagnostic tests

Test valid temporary v1-v5 files, missing files, bounded file limits, corrupt
data, BSP/map mismatch, unchanged source bytes, diagnostic reason and map
identity, and publication failure retaining the prior snapshot.

### Task 2: Implement read-only adapter loader

Read the map-derived `.nav` path through an adapter-owned file boundary, feed
bytes to the Core reader, validate supplied map/BSP identity, publish only
successful snapshots, and return bounded diagnostic records. Integrate the
load attempt into the existing map lifecycle without claiming bot movement.

### Task 3: Finalize provenance and phase verification

Register all new sources, run Windows/Linux x86 portable and Metamod tests,
manifest/artifact checks, source-format checks, FocalSpan update/query, CRG
current-head impact review, and inspect the final staged diff.

### Task 4: Commit the Phase 5 slice

Stage explicit Phase 5 paths only, run `git diff --cached --check`, and create
one atomic commit. Keep live server acceptance separate.

</tasks>

<must_haves>

- Existing `.nav` bytes are read-only and never partially published.
- Missing/corrupt/mismatched files produce explicit diagnostics.
- Six exports and Phase 4 command/actor behavior remain unchanged.
- AstraNav generation/editing/saving remains outside the phase.

</must_haves>

<verification>

1. Observe adapter tests fail before loader implementation.
2. Run all x86 tests and PE/ELF export verification.
3. Refresh FocalSpan/CRG and review final staged paths.
</verification>
