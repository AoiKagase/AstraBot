---
phase: 3
plan: 03
type: verification
wave: 3
depends_on:
  - 03-02
files_modified:
  - CMakeLists.txt
  - src/adapter/metamod/plugin_runtime.cpp
  - src/adapter/metamod/fake_client_manager.cpp
  - src/adapter/metamod/input_dispatcher.cpp
  - tests/fake_client_isolation_tests.cpp
  - tests/adapter_lifecycle_receipt_tests.cpp
  - docs/source-manifest.json
autonomous: true
requirements:
  - LIFE-02
  - LIFE-04
  - TEST-02
---

<objective>
Prove the complete FakeClient-to-input path is actor-isolated, generation
validated, bounded, and represented by receipts before moving to command/CVar
compatibility.
</objective>

<context>
Read `03-CONTEXT.md`, `03-01-SUMMARY.md`, `03-02-SUMMARY.md`, all Phase 3
plans, `AGENTS.md`, `C_CPP_REFACTOR_RULES.md`, and the pinned Metamod-P test
patterns. Treat CRG/FocalSpan as navigation/impact evidence and current
build/test output as acceptance evidence. Do not add live gameplay claims.
</context>

<tasks>

### Task 1: Write the failing multi-actor isolation tests

**Type:** test
**Files:** `tests/fake_client_isolation_tests.cpp`,
`tests/adapter_lifecycle_receipt_tests.cpp`, `CMakeLists.txt`

**Action:** Add an end-to-end fake-host fixture for two actors covering create,
  join, command enqueue, dispatch, receipt, disconnect, slot reuse, map/round
  invalidation, native guard denial, queue overflow, and post-detach callback
  safety. Assert callback counts and edict slots independently for each actor.

**Verify:** Run before adding any missing integration assertion and ensure a
  deliberately wrong actor/generation receipt fails.

**Acceptance criteria:** The test fails if commands cross actors or if stale
  input is accepted after reuse.

### Task 2: Close integration gaps and diagnostics

**Type:** implementation
**Files:** `src/adapter/metamod/plugin_runtime.cpp`,
`src/adapter/metamod/fake_client_manager.cpp`,
`src/adapter/metamod/input_dispatcher.cpp`

**Action:** Ensure map/round/disconnect/detach paths retire actor records and
  queued commands before public callbacks, emit bounded diagnostics with
  actor/slot/generation/sequence/reason, and keep native guard decisions
  visible. Ensure every engine call has a corresponding explicit receipt or
  failure result. Do not add hidden recovery or retry loops.

**Verify:** Run the integration tests through RED/GREEN/refactor and inspect
  fake callback traces for two actors.

**Acceptance criteria:** No stale or cross-actor command can produce a public
  engine call, and every failure path is observable.

### Task 3: Run Phase 3 verification harness and provenance

**Type:** verification/documentation
**Files:** `docs/source-manifest.json`, test/source files changed by this plan

**Action:** Run manifest checker, all Windows/Linux x86 CTest targets,
  Metamod ABI/hook tests, PE/ELF six-export verifier, and source-format
  checks. Refresh FocalSpan and current-head CRG. Keep generated artifacts
  out of Git and preserve dependency working trees.

**Verify:** Record actual command output and separate offline evidence from
  live HLDS/ReHLDS movement evidence.

**Acceptance criteria:** LIFE-02, LIFE-04, and TEST-02 have current offline
  evidence and no compatibility-surface code is claimed prematurely.

### Task 4: Commit the Phase 3 slice

**Type:** commit
**Files:** all intended Phase 3 source, tests, manifest, summaries, and
  verification artifacts

**Action:** Inspect staged paths, run `git diff --cached --check`, and create
  one atomic Phase 3 commit after all checks pass. Do not stage local index
  files, unrelated user changes, or SDK/reference files.

**Verify:** Inspect the resulting hash/path list and final status.

**Acceptance criteria:** The phase is complete only for offline FakeClient and
  input-dispatch behavior; live movement remains a later gate.

</tasks>

<must_haves>
<truths>
- Two actors remain isolated through create, input, disconnect, and reuse.
- Stale commands and receipts cannot reach the engine.
- Native guard denial and all engine failures are explicit.
- Provenance and x86/export checks remain green after integration changes.
</truths>
<artifacts>
- Multi-actor fake-host integration test.
- Phase 3 verification and provenance updates.
</artifacts>
<key_links>
- FakeClient manager -> actor registry -> input queue -> receipt -> engine call.
</key_links>
</must_haves>

<verification>
1. Observe isolation test fail before integration closure.
2. Run all Phase 3 focused/full tests on available x86 targets.
3. Run manifest and PE/ELF exact-export checks.
4. Refresh FocalSpan and CRG at current HEAD.
5. Check final staged paths and whitespace.
</verification>

<success_criteria>
- Offline actor and input-dispatch contracts are complete and auditable.
- Phase 4 can safely add the CSBot compatibility surface.
</success_criteria>
