---
phase: 6
plan: 06
type: feature
wave: 6
depends_on:
  - 06-05
files_modified:
  - include/astrabot/nav/nav_query.hpp
  - src/core/nav/nav_query.cpp
  - include/astrabot/runtime/nav_roam_controller.hpp
  - src/core/runtime/nav_roam_controller.cpp
  - src/adapter/metamod/plugin_runtime.hpp
  - src/adapter/metamod/plugin_runtime.cpp
  - tests/nav_roam_controller_tests.cpp
  - tests/runtime_movement_integration_contract.py
autonomous: true
requirements:
  - PAR-01
---

<objective>
Extend the existing SDK-free locomotion contracts with ZBot-derived off-mesh
recovery, deterministic basic route selection, and typed runtime movement
diagnostics without claiming live movement from intent dispatch alone.
</objective>

<context>
Use the pinned ReGameDLL-CS ZBot implementation only as a behavioral/API
comparator. Reproduce nearest-area recovery and StayOnNavMesh semantics with
AstraBot-owned value types. Keep ReGameDLL private classes, symbols, and hidden
state out of the plugin.
</context>

<must_haves>
- Off-mesh observations produce bounded intent toward the nearest valid Nav
  area instead of stopping at the old 256-unit cutoff.
- Exact-area, nearest-area, link, corridor, locomotion, and dispatch stages are
  distinguishable in Core decisions and adapter diagnostics.
- Feet-space observations are used for Nav/loco queries while raw origin is
  retained for diagnostics.
- Actor, map, round, snapshot, and command-generation validation remains
  intact.
- Offline tests pass and real-server movement remains a Phase 8 gate.
</must_haves>

<verification>
1. Run the focused NavRoamController and movement integration tests.
2. Run complete Windows x86 CTest, Phase 6 verification, manifest,
   source-format, and exact seven-export PE checks.
3. Refresh FocalSpan and code-review-graph after source changes.
4. Deploy only a SHA-matching artifact for later Windows live verification;
   do not mark live movement accepted from offline evidence.
</verification>
