---
phase: 8
plan: 02
type: verification
wave: 2
depends_on:
  - 08-01
files_modified:
  - tests/phase8_differential.py
  - tests/fixtures/phase8/README.md
  - tests/fixtures/phase8/differential-observable.json
  - docs/source-manifest.json
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - Candidate and reference traces compare only bounded observable behavior.
    - Unknown, observed absence, and observed presence remain distinct during replay.
    - A differential acceptance claim requires real pinned reference and candidate traces.
  artifacts:
    - path: tests/phase8_differential.py
      provides: Deterministic source-independent trace validator and comparator.
    - path: tests/fixtures/phase8/differential-observable.json
      provides: Versioned candidate/reference observable trace fixture.
    - path: tests/fixtures/phase8/README.md
      provides: Provenance, allowed fields, and evidence-layer instructions.
  key_links:
    - from: tests/phase8_differential.py
      to: tests/fixtures/phase8/differential-observable.json
      via: Replay harness loads, validates, aligns, and compares the fixture.
    - from: tests/phase8_differential.py
      to: .planning/REQUIREMENTS.md
      via: Deterministic result classification controls whether TEST-03 can close.
---

<objective>
Create a source-independent differential trace contract and replay review
that can compare AstraBot with the pinned CSBot reference on observable
behavior. The offline harness validates trace integrity and comparison rules;
it does not manufacture live reference behavior or claim TEST-03 complete
without an actual pinned reference trace.
</objective>

<context>
Read `08-CONTEXT.md`, the Phase 7 fixture validator and README, and the
current behavior/combat/objective/team contracts. The comparison boundary is
observable state, intents, explicit Unknown values, lifecycle transitions,
and bounded timing. It excludes private symbols, hidden coordinates, engine
receipts, delivered messages, damage success, and any copied reference code.
</context>

<tasks>

<task type="auto">
<name>Define the differential trace schema</name>
<files>tests/fixtures/phase8/README.md, tests/fixtures/phase8/differential-observable.json</files>
<action>Add a versioned fixture/readme contract with separate candidate and reference identity, provenance, map/round/tick/actor generation, observable events, and expected comparison outcomes. Require a pinned reference build identity and source-independent provenance. Bound actor/event counts, age, coordinates, durations, and trace length; reject non-finite, stale, malformed, or forbidden claims.</action>
<verify>Schema fixtures parse successfully and a malformed copy fails each bound/provenance/forbidden-field validation case.</verify>
<done>A reviewer can identify the allowed comparison surface and provenance without reading reference source.</done>
</task>

<task type="auto">
<name>Implement deterministic replay comparison</name>
<files>tests/phase8_differential.py</files>
<action>Add a standard-library-only Python harness that validates both sides, aligns records by map/round/tick/actor generation, compares only allowed observable fields, and reports mismatch paths deterministically. Preserve Unknown versus observed absence/presence and never turn an intent into an engine receipt or objective completion.</action>
<verify>Run the comparator twice on the same fixture and assert byte-stable output, then run a deliberate allowed-field mismatch and assert a nonzero result with a stable path.</verify>
<done>The replay gate distinguishes valid parity evidence, observable mismatches, and invalid/hidden inputs.</done>
</task>

<task type="auto">
<name>Exercise negative and positive cases</name>
<files>tests/phase8_differential.py, tests/fixtures/phase8/differential-observable.json</files>
<action>Run the checked-in synthetic contract fixture and add negative self-checks for missing reference provenance, hidden-state fields, stale actor generations, non-finite values, unbounded lists, and candidate/reference identity drift. If no captured reference trace exists, report the contract gate green but TEST-03 pending with the missing evidence named.</action>
<verify>Positive replay passes; every negative mutation fails closed; no result is labeled live acceptance when inputs are synthetic.</verify>
<done>Trace integrity and evidence-layer classification are executable and deterministic.</done>
</task>

<task type="auto">
<name>Register provenance and tracker state</name>
<files>docs/source-manifest.json, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
<action>Register only the new source-independent harness/fixtures in the manifest, record the pinned reference identity without importing its source, and update the tracker to distinguish a differential contract/replay harness from a completed differential acceptance set.</action>
<verify>Manifest checker and harness pass, source review finds no copied reference implementation, and TEST-03 remains pending until a real paired trace is reviewed.</verify>
<done>The repository records exactly what differential evidence exists and what remains external.</done>
</task>

</tasks>

<must_haves>

- Comparison is deterministic, bounded, actor-scoped, and source-independent.
- Hidden engine truth and success receipts cannot enter the fixture schema.
- TEST-03 is marked complete only when an actual pinned reference trace and a
  matching candidate trace have been reviewed.

</must_haves>

<verification>

1. Run positive replay and all malformed/forbidden negative cases.
2. Repeat the harness to confirm byte-stable comparison output.
3. Inspect provenance and manifest entries for copied reference material.
4. Keep live-server and multi-Bot stability requirements open.

</verification>

<success_criteria>

- The harness fails closed on invalid or hidden-state traces.
- A real reference trace can be added later without changing Core contracts.
- Current repository evidence clearly says whether TEST-03 is contract-ready
  or accepted, with no offline/live conflation.

</success_criteria>
