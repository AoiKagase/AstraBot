---
phase: 8
plan: 03
type: acceptance
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - docs/evidence/phase8-live-acceptance.md
  - docs/evidence/phase8-live/windows-x86.md
  - docs/evidence/phase8-live/linux-x86.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - PAR-06
  - TEST-04
must_haves:
  truths:
    - AstraBot loads beside unmodified ReGameDLL-CS through Metamod-P on both x86 targets.
    - Single- and multi-Bot lifecycle/gameplay runs correlate diagnostics with server state.
    - Missing server prerequisites block acceptance rather than producing synthetic passes.
  artifacts:
    - path: docs/evidence/phase8-live-acceptance.md
      provides: Cross-platform live acceptance protocol and verdict matrix.
    - path: docs/evidence/phase8-live/windows-x86.md
      provides: Windows x86 operator evidence or explicit blocker.
    - path: docs/evidence/phase8-live/linux-x86.md
      provides: Debian Linux x86 operator evidence or explicit blocker.
  key_links:
    - from: docs/evidence/phase8-live/windows-x86.md
      to: .planning/REQUIREMENTS.md
      via: Complete Windows evidence supports only the corresponding live requirement rows.
    - from: docs/evidence/phase8-live/linux-x86.md
      to: .planning/REQUIREMENTS.md
      via: Complete Linux evidence supports only the corresponding live requirement rows.
---

<objective>
Accept or explicitly block the real-server replacement behavior on Windows
x86 and Debian Linux x86 using an unmodified ReGameDLL-CS installation with
Metamod-P and the AstraBot plugin. Verify single- and multi-Bot operation,
runtime stability, and the lifecycle/gameplay surfaces left open by offline
phases.
</objective>

<context>
This is a human/operator acceptance plan. The AstraBot checkout contains no
HLDS/ReHLDS server binary or complete server installation, so the plan must
stop at its preflight checkpoint until those inputs are supplied. Do not
silently use a different server, modify ReGameDLL-CS, or treat CTest as live
proof. Do not record credentials or unrelated installation data.
</context>

<tasks>

<task type="checkpoint:human-action">
<name>Verify live prerequisites</name>
<action>Pause for operator-provided Windows x86 and Debian Linux x86 server roots. Verify architecture and identity for HLDS/ReHLDS, unmodified ReGameDLL-CS, Metamod-P, AstraBot, the BSP, and compatible read-only `.nav`; record versions/commits and configuration hashes without secrets.</action>
<instructions>Do not modify ReGameDLL-CS or use a different server. Supply the prerequisite checklist and the two platform evidence paths, or state the exact missing root/binary/configuration.</instructions>
<verification>Each platform has a complete identity checklist, or an explicit blocked record naming the missing prerequisite.</verification>
<resume-signal>Resume when both test roots are supplied and verified, or when both platform blockers are documented.</resume-signal>
</task>

<task type="auto">
<name>Run single-Bot lifecycle acceptance</name>
<files>docs/evidence/phase8-live/windows-x86.md, docs/evidence/phase8-live/linux-x86.md</files>
<action>With the pinned map/configuration, load the plugin, create/join/control/remove one Bot, and observe Nav loading, locomotion, perception, combat/objective intents, radio/chatter, round restart, death/respawn, map change, disconnect, reconnect, and slot reuse. Capture server log, plugin diagnostics, actor/map/round/tick/generation identities, and failure details for each OS.</action>
<verify>Evidence records the exact server/plugin/map/Nav/config identity and each lifecycle criterion as pass, fail, blocked, or not run.</verify>
<done>Single-Bot behavior is classified separately for Windows and Debian Linux without inferring from offline tests.</done>
</task>

<task type="auto">
<name>Run multi-Bot stability and gameplay acceptance</name>
<files>docs/evidence/phase8-live/windows-x86.md, docs/evidence/phase8-live/linux-x86.md</files>
<action>Repeat with the configured multi-Bot count and a two-team scenario. Check actor-specific state, command dispatch, objective/team behavior, no stale slot commands, no native-Bot mixing, no crash/hang, and bounded CPU/log behavior across round and map transitions. Capture exact test duration and Bot count for both OS targets.</action>
<verify>Logs and diagnostics correlate all active generations and show no crash/hang/stale command; incomplete runs remain not accepted.</verify>
<done>Both platform reports contain multi-Bot stability evidence or an explicit failure/blocker.</done>
</task>

<task type="auto">
<name>Gate the result</name>
<files>docs/evidence/phase8-live-acceptance.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
<action>Classify every criterion as passed, failed, blocked, or not run. Mark PAR-06/TEST-04 only when both OS targets have complete evidence. If prerequisites are unavailable, write a precise blocked report and leave all live requirements pending; never create synthetic server logs or infer live parity from offline results.</action>
<verify>Acceptance matrix agrees with both platform reports and requirement rows; no live requirement is closed from CTest, fixtures, CRG, or FocalSpan.</verify>
<done>The live gate has a human-auditable verdict or a reproducible external-input blocker.</done>
</task>

</tasks>

<must_haves>

- Both Windows x86 and Debian Linux x86 real-server runs are represented.
- The ReGameDLL-CS installation remains unmodified and AstraBot is loaded as a
  Metamod-P plugin beside it.
- Single- and multi-Bot lifecycle/stability evidence includes map/round/tick,
  actor generation, Bot count, and failure details.
- Missing external inputs produce a blocked acceptance record, not a pass.

</must_haves>

<verification>

1. Complete the prerequisite and identity checklist for each OS.
2. Capture and review single- and multi-Bot runs across lifecycle transitions.
3. Correlate diagnostics with server logs and inspect for crashes/hangs/stale
   commands.
4. Apply the explicit checkpoint classification before changing requirements.

</verification>

<success_criteria>

- TEST-04 has complete, reproducible evidence for both OS targets, or its
  blocker is recorded with the exact missing input.
- PAR-06 is accepted only after lifecycle and multi-Bot stability evidence on
  both targets.
- No live claim relies solely on offline CTest, fixtures, CRG, or FocalSpan.

</success_criteria>
