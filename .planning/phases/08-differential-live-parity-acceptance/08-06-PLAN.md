---
phase: 8
plan: 06
type: gap-closure
wave: 6
gap_closure: true
gap_ids: [nav-roam-stuck, live-bot-combat-missing, live-c4-objective-missing]
depends_on:
  - 08-05
files_modified:
  - src/core/nav/locomotion.cpp
  - src/core/runtime/nav_roam_controller.cpp
  - src/adapter/metamod/plugin_runtime.cpp
  - tests/nav_roam_controller_tests.cpp
  - tests/compat_actor_command_tests.cpp
  - tools/verify-phase8-live-log.ps1
  - docs/evidence/phase8-live/windows-x86.md
autonomous: false
requirements:
  - PAR-01
  - PAR-03
  - PAR-04
  - PAR-06
  - TEST-04
---

<objective>
Resolve the next live acceptance boundary after TeamInfo/readiness: the
current-source deployed run reaches `ready=1` but NavRoam returns
`roam_no_intent` with `LocomotionResult::Stuck`, and the fresh log interval
contains no Bot-to-Bot attack or C4 plant/defuse event. Separate Nav/physics
failure from missing live combat/objective adapter wiring and keep each log
gate independently verifiable.
</objective>

<context>
Fresh qconsole lines beginning at 77406 use the rebuilt DLL and mixed CT/T
Bots. The strict verifier excludes spawn teleports and reports movement
failed, combat failed with zero Bot-to-Bot attacks, and C4 failed with zero
plant/defuse lines. AstraBot adapter sources currently contain movement
dispatch but no obvious combat/objective dispatch call path; Core combat and
objective contracts are offline-only until an adapter boundary is proven.
</context>

<tasks>

<task type="auto">
<name>Reproduce and isolate NavRoam Stuck</name>
<files>tests/nav_roam_controller_tests.cpp, src/core/nav/locomotion.cpp, src/core/runtime/nav_roam_controller.cpp</files>
<action>Add a regression scenario using the live decision fields: valid
current area, loaded Nav snapshot, spawn-settled observation, and the first
post-spawn movement feedback. Determine whether `Stuck` comes from progress
sampling before a valid intent, invalid corridor/link selection, or real
physics feedback. Fix only the confirmed cause and preserve explicit
`roam_no_intent` diagnostics.</action>
<verify>The focused test is RED before the fix and GREEN after it; the result
emits a valid movement intent or an explicit bounded recovery decision rather
than silently resetting to neutral input.</verify>
<done>Nav/physics failure is isolated from combat/objective acceptance.</done>
</task>

<task type="auto">
<name>Trace live combat and objective adapter boundaries</name>
<files>src/adapter/metamod/plugin_runtime.cpp, tests/compat_actor_command_tests.cpp</files>
<action>Trace Core combat decisions through the adapter and verify whether a
valid fire/reload intent can reach `RunPlayerMove`/weapon commands. Trace
RoundObjectivePlanner Plant/Defuse proposals through the adapter and verify
whether GameDLL-visible bomb actions exist. Add focused contract tests for
each boundary. If no production path exists, record the exact missing
adapter boundary rather than treating offline Core proposals as live action.</action>
<verify>Tests either prove an adapter dispatch path or fail with a precise
missing-boundary diagnostic. No live combat/C4 claim is made from Core-only
tests.</verify>
<done>PAR-03/PAR-04 live blockers are explicit and actionable.</done>
</task>

<task type="auto">
<name>Repeat offset-scoped live log gates</name>
<files>tools/verify-phase8-live-log.ps1, docs/evidence/phase8-live/windows-x86.md</files>
<action>After the pinned artifact is rebuilt and deployed, capture a new log
offset and run the verifier for movement, Bot-to-Bot damage, and both C4
events. Keep spawn teleports, historical log lines, `Spawned_With_The_Bomb`,
and `Target_Saved` excluded from pass criteria.</action>
<verify>The JSON result reports all three gates independently with exact
line-offset scope, diagnostic evidence, and no idle-kick during observation.</verify>
<done>Windows log evidence is pass/fail per behavior and ready for UAT update.</done>
</task>

</tasks>

<must_haves>

- `ready=1` is not treated as movement success without post-spawn horizontal progress.
- Bot combat requires a fresh Bot-to-Bot damage line.
- C4 objective success requires fresh Bot plant and defuse events.
- Core-only contracts never close live PAR-03/PAR-04.
- Linux x86, pinned differential trace, and full PAR-06 remain separate gates.

</must_haves>

<verification>

1. Reproduce the live `Stuck` decision with a focused current-source test.
2. Isolate and fix or explicitly classify Nav/physics cause.
3. Prove or document the combat/objective adapter dispatch boundary.
4. Rebuild, pin, deploy, and run the offset-scoped log verifier.
5. Update UAT and Phase 8 verification without promoting missing gates.

</verification>
