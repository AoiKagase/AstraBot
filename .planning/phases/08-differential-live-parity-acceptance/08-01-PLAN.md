---
phase: 8
plan: 01
type: verification
wave: 1
depends_on: []
files_modified:
  - tests/x86_artifact_verifier_tests.py
  - tools/verify_x86_artifact.py
  - docs/source-manifest.json
  - tests/fixtures/phase8/08-01-artifact-matrix.json
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TEST-01
  - TEST-04
must_haves:
  truths:
    - Current Windows x86 and Debian Linux x86 artifacts have reproducible build identity.
    - Each Metamod artifact is the expected PE/ELF x86 format with exactly six exports.
    - Offline evidence remains explicitly separate from live-server acceptance.
  artifacts:
    - path: tests/x86_artifact_verifier_tests.py
      provides: Regression coverage for artifact format, architecture, and export rejection.
    - path: tools/verify_x86_artifact.py
      provides: Deterministic PE/ELF x86 and exact-export checker.
    - path: tests/fixtures/phase8/08-01-artifact-matrix.json
      provides: Versioned artifact evidence and command/build identity record.
  key_links:
    - from: tests/x86_artifact_verifier_tests.py
      to: tools/verify_x86_artifact.py
      via: Python subprocess invokes the checker for each current artifact.
    - from: tests/fixtures/phase8/08-01-artifact-matrix.json
      to: .planning/REQUIREMENTS.md
      via: Evidence rows identify the offline result without closing live requirements.
---

<objective>
Freeze reproducible Windows x86 and Debian Linux x86 artifact evidence for
the current AstraBot implementation, including exact Metamod exports and the
offline regression matrix. Keep this plan's result separate from live-server
acceptance.
</objective>

<context>
Read `08-CONTEXT.md`, the current CMake targets, `tests/x86_artifact_verifier_tests.py`,
`tools/verify_x86_artifact.py`, and the latest Phase 7 offline gate. Preserve
the existing six-export ABI and do not stage generated FocalSpan state.
</context>

<tasks>

<task type="auto">
<name>Establish the artifact matrix</name>
<files>build-portable-x86-test, build-metamod-x86-test, build-linux-x86-test, build-linux-x86-metamod-test, tests/fixtures/phase8/08-01-artifact-matrix.json</files>
<action>Inspect the existing Windows and Debian x86 build directories and record the commit, compiler/generator, build type, artifact path, PE/ELF format, and whether the portable and Metamod targets are present. If a required artifact is missing, report it as a failed prerequisite instead of substituting an old binary without its build identity.</action>
<verify>Fresh build identity and artifact existence checks resolve every recorded path, or return a named missing-prerequisite failure.</verify>
<done>The versioned matrix identifies each available current-HEAD artifact and its exact platform/build identity.</done>
</task>

<task type="auto">
<name>Run focused and complete offline checks</name>
<files>CMake build directories, tests/x86_artifact_verifier_tests.py, tools/verify_x86_artifact.py, docs/source-manifest.json</files>
<action>Run the portable and Metamod CTest suites for both x86 environments, source manifest and C/C++ format checks, Python verification harnesses, and the PE/ELF checker against each current Metamod artifact. Capture exit codes, test counts, toolchain identity, and exact HEAD in the matrix.</action>
<verify>Every invoked command has a fresh exit code and the artifact checker rejects missing, wrong-format, wrong-architecture, and export-set mismatch fixtures.</verify>
<done>Available current artifacts and the offline regression matrix pass with reproducible command evidence.</done>
</task>

<task type="auto">
<name>Audit ABI and source boundaries</name>
<files>src/adapter/metamod/plugin_exports.cpp, include/astrabot/metamod/abi_contract.hpp, CMakeLists.txt, docs/source-manifest.json</files>
<action>Confirm the six exports are exactly Meta_Query, Meta_Attach, Meta_Detach, GetEntityAPI2, GetEngineFunctions, and GiveFnptrsToDll; confirm Core has no SDK dependency and the Metamod adapter remains the only SDK-facing target. Treat any mismatch as a defect requiring a focused implementation plan.</action>
<verify>PE/ELF export inspection, manifest/source-boundary checks, and direct CMake target inspection agree for both platform artifacts.</verify>
<done>The ABI and Core/adapter boundary is evidenced without changing the public export contract.</done>
</task>

<task type="auto">
<name>Record the offline checkpoint</name>
<files>tests/fixtures/phase8/08-01-artifact-matrix.json, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
<action>Update only tracker and evidence documents supported by fresh output. Mark no live requirement complete, run git diff --check, inspect explicit paths, and leave PAR-06/TEST-03/TEST-04 pending unless their separate plans produce their own evidence.</action>
<verify>Git diff/check output is clean and tracker text still distinguishes offline artifacts from differential and live acceptance.</verify>
<done>08-01 has a reviewable offline checkpoint and no generated FocalSpan state is staged.</done>
</task>

</tasks>

<must_haves>

- Both supported x86 artifact families are checked independently.
- Export verification rejects missing, wrong-architecture, wrong-format, and
  extra/missing-export artifacts.
- Offline evidence is not described as real-server or full CSBot acceptance.

</must_haves>

<verification>

1. Run the exact current-HEAD build/test commands for Windows and Debian x86.
2. Run the artifact verifier and Python tests with fresh exit-code evidence.
3. Confirm staged/diff paths contain no generated FocalSpan database/config.
4. Confirm tracker language still distinguishes offline and live acceptance.

</verification>

<success_criteria>

- A reviewer can reproduce the artifact matrix from the recorded commands.
- All available current artifacts pass x86/exact-export validation.
- Missing live inputs, if any, are explicit and TEST-04 remains open.

</success_criteria>
