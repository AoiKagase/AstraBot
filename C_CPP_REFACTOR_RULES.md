# C/C++ コーディング・リファクタリング規約

この規約は、依頼された範囲のC/C++コードを、動作と互換性を維持して整理するための指示である。
規約の存在だけを理由に、無関係なコードの変更、全面的な再設計、コミットやpushを行わない。

## 1. 適用範囲と規則の扱い

- 適用対象は、保守対象の自作コード・既存コード・Codexが生成または修正したC/C++コード。AI生成を免除理由にしない。
- ソース、ヘッダー、インライン実装、テンプレート実装、テストを含む。拡張子だけでなく内容と用途で判断する。
- 外部ライブラリ、SDK、ベンダー由来コード、submoduleは、明示的な依頼がない限り改変しない。由来を確認し、ディレクトリ名だけで除外しない。
- 通常のコード生成器が繰り返し生成する成果物は、可能ならテンプレートや生成元を修正して再生成する。Codexが書いた通常のソースとは区別する。
- ユーザーの最新の明示指示、および適用される上位・下位の指示ファイルを確認する。矛盾を隠して規約ファイルを書き換えない。
- 「必須」は遵守する。「推奨」は可読性・互換性・既存の明示規約に応じて判断し、重要な不採用理由を報告する。
- 未確定の命名形式、コメント言語、ポインター／参照の宣言位置を独断で統一しない。判断できない項目だけ保留し、他の安全な修正は進める。

## 2. 動作・互換性の保護【必須】

- リファクタリングに機能追加、仕様変更、依存ライブラリ更新、言語規格の引き上げを混ぜない。
- 公開API、ABI、export名、呼び出し規約、構造体レイアウト、列挙値、ファイル／通信形式、設定キー、外部から参照される名前を保持する。
- 評価順序、副作用、短絡評価、寿命、所有権、例外、ロック範囲、エラー処理、数値型・精度を変えない。
- 「書式整理のついで」に所有権を変更したり、ポインターをスマートポインターへ全面置換したりしない。
- 原因が分からないコードを不要と決めつけて削除しない。不具合を発見したら、今回の整理と区別して報告する。
- ユーザーの未コミット変更・未追跡ファイル・設定を保護する。無断のreset、clean、stash、全ファイル上書き、履歴改変を行わない。
- 自分の変更を戻す場合も、自分が追加した変更だけを対象にする。既存変更との衝突箇所は無理に改変しない。
- 正規表現だけで識別子・演算子・数値を一括置換しない。コメント、文字列、マクロ、別スコープの同名要素を区別する。

## 3. ファイルの保存形式【必須】

- インデントは実タブ文字（U+0009）を使い、表示幅は4とする。スペース4個による代用は禁止。
- ネストの段下げはタブで表現する。継続行もタブを基本とし、必要最小限の桁合わせに限りスペースを許可する。
- 改行コードはLFに統一する。CRLFや単独CRを混在させない。
- 文字コードはUTF-8、BOMなしとする。
- ファイル末尾は改行で終える。不要な末尾空行は増やさない。
- 行末のスペース・タブは禁止。空行は空文字列とし、インデントを残さない。
- 既存ファイルを変換するときは、元の文字コードとコンパイラーの文字セット設定を確認する。文字化けを置換文字でごまかさない。
- 文字列リテラル、raw string、埋め込みデータの意味を保存する。保存形式の変更が実行時の文字列やバイト列へ影響する場合は、同値性を検証するか、その箇所を理由付きで保留する。

## 4. 行・空白・空行

### 4.1 1行1命令【必須】

- 1行には原則として1つの文だけを書く。複数の処理を同じ行へ詰め込まない。
- `if`などの制御構文と、その本体の処理を同じ行に書かない。
- 長い1つの文を複数行へ折り返すことは許可する。
- `for`のヘッダーに含まれる初期化・条件・更新は例外とする。例外を利用して余分な処理を詰め込まない。
- カンマ演算子や連鎖代入で、独立した処理を無理に1文へまとめない。
- 【推奨】変数宣言も1行1変数とする。ただし構造化束縛など、構文上ひとまとまりの宣言を機械的に分割しない。

### 4.2 演算子・区切りの空白【必須】

- 代入、算術、比較、論理、ビット演算などの二項演算子の前後には、半角スペースを1つ入れる。
- 三項演算子の`?`と`:`の前後も空ける。
- 単項演算子はオペランドに密着させる。例：`++index`、`!isReady`、`-offset`、`*pointer`、`&value`。
- メンバーアクセス・スコープ解決は空けない。例：`object.member`、`pointer->member`、`std::size_t`。
- カンマの前は空けず、同じ行に次の要素が続く場合は後ろを半角スペース1つ空ける。
- カンマ直後に改行する場合は、スペースを追加しない。行末空白禁止を優先する。
- 演算子と、宣言・テンプレート引数・マクロの記号を混同しない。`Type* value`と`Type *value`などの未確定項目は既存方式を維持する。
- 文字列内部へ空白規則を適用しない。
- 【推奨】`if (`、`for (`、`while (`、`switch (`のように制御キーワードの後を空ける。関数名と`(`は密着させる。

### 4.3 空行【必須・推奨】

- 【必須】関数定義の間、および目的が異なる処理のまとまりの間に空行を入れる。
- 【推奨】通常は空行1行とし、意味のない連続空行を作らない。
- 【推奨】各文の間へ機械的に空行を入れない。密接な初期化・検証・更新などは、意味に応じてまとめる。

### 4.4 行の長さ【推奨】

- インデントを含めて120桁以内を推奨上限とする。タブストップは4桁で扱う。
- 120桁は絶対上限ではない。引数列、条件式、式の意味の区切りで折り返す。
- 文字数を減らすためだけの不明瞭な略語や、意味のない中間変数を導入しない。
- URL、原文を保持すべき文字列、対応関係が重要なデータ表などは、可読性や内容保持を優先して超過を許容する。

## 5. 波括弧【必須】

- ブロックの`{`と`}`は、それぞれ独立した行へ置く。Allman形式とする。
- 関数、制御構文、クラス、構造体、列挙型、名前空間、ラムダのブロックに適用する。
- `else`、`catch`なども別行へ置く。空のブロックや短い関数でも、ブロック全体を1行に畳まない。
- 文法上必要な終端の`};`、ラムダを引数に渡す場合の`});`などは同じ行に置いてよい。閉じ括弧の後へ無関係な処理を書かない。
- マクロ継続のバックスラッシュや名前空間終了コメントは、文法・識別上必要な付記として許可する。
- `Type value{};`などの初期化式はブロックではないため、短いものは同じ行に置いてよい。意味を確認せず初期化方式を変更しない。
- 【推奨】制御構文の本体が1文でも波括弧を省略しない。追加時はスコープやマクロ展開の影響を確認する。

例（字下げは実タブ。識別子の表記方式を固定する例ではない）：

```cpp
bool isWithinRange(int value, int minimum, int maximum)
{
	if (minimum > maximum)
	{
		return false;
	}

	return value >= minimum && value <= maximum;
}
```

```cpp
if (isReady)
{
	startProcessing();
}
else
{
	stopProcessing();
}
```

## 6. ファイル名・関数名・変数名

### 6.1 命名形式の決定方法【必須】

- ファイル名、型名、関数名、変数名、メンバー名、定数名、マクロ名、名前空間を区別して調査する。
- `snake_case`、`camelCase`、`PascalCase`、`m_`、`k`などは、この規約では新たに固定しない。
- 既存の明示規約を優先する。明示規約がなければ、対象モジュールの一貫した慣習を採用する。
- 慣習が混在して判断できない場合は、一括改名を保留する。採用した方式と保留した対象を短く報告する。
- CとC++で命名方式が異なっていても、理由を確認せず統一しない。
- コメント言語、Doxygen形式、ポインター／参照の宣言位置も、同じ原則で扱う。

### 6.2 名前の意味と変更【必須・推奨】

- 【推奨】ファイル名は責務、関数名は動作、変数名は内容を表す。真偽値は肯定・否定を読み取れる名前にする。
- 【推奨】時間・距離・容量などは、単位が曖昧なら名前や型で示す。型で明確なら無意味な重複は避ける。
- 【推奨】ヘッダーと対応する実装は基本名をそろえる。ただし、既存のファイル配置・分割方針を尊重する。
- 【必須】`.h`と`.hpp`、テストの接尾辞、メンバーの接頭辞などを、好みだけで一括変更しない。
- 【必須】改名は参照範囲を追跡でき、動作と外部契約を保持できる場合に限る。外部公開名を見た目のために変えない。
- 【必須】ファイル改名時は、include、ビルド定義、プロジェクトファイル、テスト、スクリプトなどの参照を確認する。
- 【必須】大文字・小文字だけの改名も含め、対応OS間のパス差異を確認する。ソース内の単純置換だけで完了としない。

## 7. コメント

- 処理の目的、設計意図、制約、不変条件、例外的対応の理由を説明する。
- 複雑な分岐、境界条件、外部仕様との対応、所有権や寿命が分かりにくい部分には、必要な説明を補う。
- コードを日本語や英語へ置き換えただけのコメントを各行へ量産しない。
- 公開関数や契約が分かりにくい関数は、必要に応じて引数の制約、戻り値、失敗時の動作、所有権、副作用を説明する。
- 実装を変更したら関連コメントも更新する。未確認の意図・仕様・経緯を推測して事実として記載しない。
- 新しいコメントは対象モジュールの言語・形式に合わせる。日本語化、英語化、Doxygen化だけの全体書き換えはしない。
- ライセンス・著作権表記・必要な帰属表示は保持する。
- TODOを追加する場合は、未完了事項と条件を具体的に書く。規約違反をTODOだけで修正済みにしない。

## 8. 定数・数値リテラル【必須：マジックナンバー禁止】

- 仕様上の意味を持つ数値を説明なく直接埋め込まない。回数上限、タイムアウト、サイズ、閾値、状態番号、ビットマスクなどは意味のある名前で表す。
- 名前付き定数、列挙値、既存APIの定数など、対象言語と用途に合う表現を選ぶ。
- 定数名は目的や意味を示す。`kThree = 3`のような数値の言い換えは禁止。
- 同じ値でも意味が異なるものは別の定数にする。逆に同じ仕様を表す値は、不必要に重複定義しない。
- 定数は必要最小限のスコープへ置く。無関係な定数を巨大な共通ヘッダーへ集約しない。
- 元の値、符号、型、整数／浮動小数点の区別、サフィックス、単位、精度を保持する。定数化に伴う暗黙変換やオーバーロード選択の変化を確認する。
- C++では既存の言語規格で使える場合に`constexpr`を候補とする。Cでは用途に応じて`enum`、`static const`、マクロなどを選び、整数定数式が必要な箇所を壊さない。
- プリプロセッサーの`#if`などで使う値は、通常の変数へ機械的に置換しない。ヘッダーの定義はリンケージや多重定義も確認する。
- ループ開始の`0`、単純な増分の`1`など、意味が明白な言語的・数学的用途は許可する。ただし`0`、`1`、`-1`などを値だけで一律除外しない。状態番号や仕様値なら名前を付ける。
- テストの期待値を定数化する場合、実装側の同じ定数を流用して誤りを検出できなくしない。独立した期待値として意味を示す。
- 標準表、既知のバイト列、テスト入力データは、データ全体の名前・目的・必要な出典を示す。数値1個ずつに無意味な名前を付けない。
- 数値の意味を追跡できない場合は、推測した定数名を付けない。場所と不足する根拠を報告し、未対応として扱う。

## 9. ヘッダー構成【推奨】

- 通常のヘッダーは、単独でincludeして宣言に必要な型などがそろう自己完結型にする。偶然の推移的includeへ依存しない。
- 再includeを意図した定義断片・Xマクロ用ファイルなどは、通常のヘッダーと区別して扱う。
- 新規の通常ヘッダーで既存規約がない場合は、`#ifndef`／`#define`／`#endif`によるインクルードガードを推奨する。
- ガード名はプロジェクト名・必要なパス・ファイル名から衝突しにくい名前にする。予約識別子を使わない。
- 既存の`#pragma once`が採用されている場合は、形式変更だけを目的に全面置換しない。二重の保護を習慣で追加しない。
- 公開に必要な宣言をヘッダーへ置き、内部限定の実装は実装側へ寄せる。ただしテンプレート、inline、header-only構成などの必要な定義を機械的に移動しない。
- ヘッダーのグローバルスコープへ`using namespace`を追加しない。既存のものを除去する場合は利用側への影響を確認する。
- 不要なincludeの削除や前方宣言への変更は、完全型の必要性と各ビルド構成への影響を確認して行う。
- C公開APIの`extern "C"`などは、既存の公開契約とC/C++両側の互換性を守る。今回の整理だけを理由に公開契約を変更しない。

実装ファイルのincludeは、依存上の制約がなければ次のグループ順を推奨する。
グループ間は空行1行、各グループ内は名前順を基本とする。

```text
対応する自分のヘッダー

標準ライブラリ

OS・外部ライブラリ

プロジェクト内のその他のヘッダー
```

プリコンパイル済みヘッダー、feature-test macro、OSヘッダーなどで順序制約がある場合は、
コンパイル条件を優先する。includeの移動・並べ替えを単なる空白変更と同じ安全性で扱わない。

## 10. ツール設定

- 既存の`.editorconfig`、`.clang-format`、`.clang-tidy`、`.gitattributes`と、下位ディレクトリの設定を確認する。
- 設定が本規約と矛盾する場合は、影響範囲を確認して対象言語・対象ディレクトリに限って調整する。既存設定をテンプレートで丸ごと置き換えない。
- フォーマッターを使う場合は、実行バージョンと設定の対応を確認し、少数の代表ファイルで差分を確認してから範囲を広げる。
- フォーマッターが命名、コメントの意味、所有権、マジックナンバーの意味まで保証すると考えない。
- 静的解析の自動修正を無差別に適用しない。検査を通すために警告を全体無効化したり、抑制コメントをばらまいたりしない。
- `.editorconfig`だけで改行・文字コードの既存ファイル変換が済んだとみなさない。保存後の実ファイルを確認する。
- LFへ統一したファイルが再びCRLFになる設定があれば確認する。無断でユーザーのグローバルGit設定やエディター設定を変更しない。

## 11. リファクタリングの進め方

1. **現状確認**：適用される指示、対象範囲、未コミット変更、言語規格、対応環境、ビルド・テスト方法を確認する。Gitなら`git status --short`などで開始時の状態を把握する。
2. **対象の把握**：対象ファイルを列挙し、外部由来・通常生成物・自作コードを分類する。全体対応を依頼された場合は、少数ファイルだけで全体完了としない。
3. **基準の記録**：採用する既存命名方式と例外、実行可能な基準ビルド・テストの結果を短く記録する。既存の失敗と今回の変更後の失敗を区別する。
4. **小さな単位で実装**：保存形式・空白・括弧の整理と、命名・定数化・ヘッダー整理・構造変更を分ける。まず動作を変えない規約適用を優先する。
5. **関連箇所の検証**：変更したモジュールと利用側を確認し、必要なビルド・テストを実行する。定数化や改名は参照・型・境界条件も確認する。
6. **仕上げ**：最終差分、保存形式、規約違反、対象範囲を確認し、最後の変更後の状態で必要な検証を行う。安全に進められる作業を実施し、計画提示だけで完了としない。
7. **報告**：完了、未対応、理由付き例外、未検証を区別し、実行した検証結果とともに報告する。

運用上の注意：

- 既存コードも対象だが、変更範囲は依頼に従う。全体対応を依頼された場合は、段階的に進めて残件を管理する。
- 大きな関数分割や設計変更は、今回の目的に必要で、同値性を確認できる場合に限る。
- 純粋な書式変更だけのために大量の新規テストを作らない。意味へ影響し得る変更には、必要に応じて焦点を絞った検証を追加する。
- 全テストを1行変更ごと、あるいはGit操作のたびに無条件で繰り返さない。関連テストを区切りで実行し、最後に必要な全体検証を行う。
- 利用できないツールや対象OSを、検証済みとして扱わない。環境制約があっても安全な作業は進め、検証できない影響範囲を残す。
- コミット、push、merge、ブランチ削除は、ユーザーが明示的に依頼した場合だけ行う。

## 12. 完了判定と報告形式

### 完了前の確認

- 変更したC/C++ファイルが、実タブ・表示幅4、LF、UTF-8 BOMなし、末尾改行あり、行末空白なしになっている。
- 1行1命令、演算子とカンマの空白、ブロック括弧の独立行、関数間・処理のまとまり間の空行を確認した。
- 120桁の推奨、命名、コメント、マジックナンバー、ヘッダーの適用状況と例外を確認した。
- 公開契約、数値型・値、評価順序などが保持されていることを、変更内容に応じて確認した。
- 関連する参照、ビルド定義、対応環境、テストを確認した。確認できていないものを列挙した。
- Git管理の場合は`git diff --check`などで差分の空白異常を調べた。ステージ済み差分と新規・未追跡ファイルも必要に応じて別途確認した。
- ユーザーの既存変更を保持し、無関係なファイル変更・生成物混入・不要な設定変更がないことを確認した。

### 最終報告

長い作業ログを貼らず、次の項目を簡潔に報告する。

```text
変更概要：対象範囲と実施した整理
採用方式：既存から引き継いだ命名・コメント・宣言形式
対象状況：完了／未対応／対象外と、それぞれの理由
検証結果：実行コマンド、成功・失敗、既存失敗との区別
残件・例外：場所、理由、必要な追加確認
未検証範囲：対象OS、ビルド構成、実行時動作など
```

実行していないビルド・テストを成功と書かない。
静的な確認やフォーマッターの成功だけで、動作互換性を保証したと書かない。
未対応・未検証が残る場合は、その範囲を示し、全体完了と断定しない。
