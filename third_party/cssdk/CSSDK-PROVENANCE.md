# CSSDK provenance

This directory is a verbatim vendor snapshot copied from:

`H:/sourcecode/003.Game/amxmodx/AstraBot_bk/third_party/cssdk`

The snapshot is used only as an adapter-side public SDK boundary for the
participation compatibility work. `LICENSE` and `README.md` are preserved
from the source snapshot. AstraBot does not compile or link a ReGameDLL-CS
implementation, copy private bot behavior, or expose CSSDK types through the
SDK-free Core target.

The current AstraBot join controller is independently maintained and uses
public Metamod-P/HLSDK callbacks and message fields only. The vendor tree is
read-only input for this boundary; it is not modified at build or runtime.
